# studyos

## Module Onboarding Checklist

- Neue Datei `data/[modul].js` anlegen.
- Datei in `index.html` **vor** `core.js` einbinden.
- Eindeutige `module.id`, `topic.id` und `subtopic.id` verwenden.
- `legacyKey`-Werte eindeutig halten.
- `QUESTIONS.topic` muss zu einer existierenden `topic.id` passen.
